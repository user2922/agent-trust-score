# Secrets and local config — never commit these
.env
.env.*
!.env.example

# Credential material (BR-03: this tool flagged its own repo for missing these)
*.pem
*.key
*.p12
*.pfx
*.keystore
credentials
credentials.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# This tool's own output and cache
report.md
report.json
report.html
.cache/
dist/
site/
